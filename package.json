{
  "name": "show-up",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 5173",
    "test": "node --test tests/model.test.js",
    "check": "node --check app.js && node --check model.js && node --check db.js && node --check sw.js"
  }
}
